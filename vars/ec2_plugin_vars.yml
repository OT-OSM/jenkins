---
cloud_name: "ec2-dynamic-cloud"
region: "us-east-1"

ami_id: "ami-dummy"
template_description: "On-demand Jenkins EC2 agent"

iam_instance_profile_arn: "dummy-profile"

label: "ec2-agent"
executors: 1
remote_admin: "ubuntu"

# ❌ dummy placeholders (never used at runtime)
subnet_id: "dummy-subnet"
sg_id: "dummy-sg"

instance_type: "t4g.small"

minimum_instances: 0
minimum_spare: 0
idle_temination_timeout: 30
instance_cap: 5

use_instance_profile: true

tags:
  Name: "jenkins-ec2-agent"
  Environment: "dummy"
  Owner: "dummy"
